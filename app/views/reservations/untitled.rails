# if :check_dates.valid? 
			# 	flash[:error] = "start dates should be before end date"
			# elsif :check_dates_today.valid?
			# 	# flash[:error] = "start dates cannot be before today"
			# end


			<div class="demo-frame">

   <%= link_to 'search', listings_search_path %>

   # @listing = Listing.where(nil)
		# filtering_params(params).each do |key, value|
		# 	@listing = @listing.public_send(key, value) if value.present?
		# end

		

	